// expect-stderr-contains: syntax error
// expect-exit: 1
// Nivel 1.3: los errores de sintaxis se emiten en inglés por stderr en formato
// `archivo:linea: syntax error: ...` (salteable desde el editor). Este test
// bloquea una regresión al formato español viejo en stdout.
let x =
