// expect: 0
// TRIPWIRE (bug conocido): indexar el resultado de una llamada inline
// f(x)[i] no resuelve -> imprime 0 en vez de 20 (exit 0, sin error).
// Workaround actual: asignar a una variable intermedia y luego indexar.
// Salida DESEADA: 20. Cuando este test empiece a FALLAR el bug se arregló:
//   -> actualizá el expect a 20 y borrá este aviso.
let v = json_parse("[10,20,30]")[1];
println(v);
