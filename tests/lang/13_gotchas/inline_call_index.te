// expect: 20
// Gotcha ARREGLADO: indexar el resultado de una llamada inline f(x)[i] ahora
// resuelve. El fix extiende resolve_to_list (ast.c) para ejecutar la llamada
// (CALL_FUNC/CALL_METHOD/...) y leer el resultado LIST desde __ret__.
let v = json_parse("[10,20,30]")[1];
println(v);
