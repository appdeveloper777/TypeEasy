// expect: 3
// Gotcha ARREGLADO: s.split(sep) ahora devuelve la longitud real (3).
// El fix usa te_list_append en te_string.c para mantener el índice lateral
// (TEListIdx) sincronizado, de modo que .length / list_length reflejan el real.
let s = "a,b,c";
let parts = s.split(",");
println(parts.length);
