// expect: oob=null
// REGRESIÓN POSITIVA (antes era un TRIPWIRE tipo B).
// Indexar fuera de rango `xs[99]` ahora devuelve null de primera clase en vez
// de colapsar silenciosamente a INT 0. Esto habilita `xs[99] ?? default`.
let xs = [10, 20, 30];
let v = xs[99];
println($"oob={v}");
