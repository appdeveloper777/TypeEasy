// expect: 15
// Retornar una función desde otra función preserva la captura por valor.
// make(10) devuelve un sumador; add10(5) == 15.
var make = fn(n) => fn(x) => x + n;
let add10 = make(10);
println(add10(5));
