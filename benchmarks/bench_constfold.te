class Calc:
    var base : int = 7
    method compute(x : int) : int
        return x * 3 + 2

const c = Calc()
const limit : int = 100
var i : int = 0
var total : int = 0
for i in 0 to limit
    total = total + c.compute(i)
print(total)
