// Benchmark 3: while + break + continue + lógicos
// Mide: control de flujo nuevo (Fase 4 y 5)
var i = 0;
var sum = 0;
while (i < 100000) {
    i = i + 1;
    if (i > 50000 && i < 60000) { continue; }
    if (i > 80000) { break; }
    sum = sum + i;
}
println(sum);
