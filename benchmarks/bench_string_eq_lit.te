/* Bench: comparaciones entre literales repetidos.
 * Caso ideal para interning: ambos lados son literales interned, pointer-eq.
 */
int hits = 0;

for(i = 0; 10000000; 1) {
    if("hola_mundo" == "hola_mundo") {
        hits = hits + 1;
    }
}

println(hits);
