/* Bench string-heavy: 10M comparaciones de strings.
 * Sin interning: cada == hace strcmp.
 * Con interning: cada == compara punteros.
 */
string target = "hola_mundo";
int hits = 0;

for(i = 0; 10000000; 1) {
    if(target == "hola_mundo") {
        hits = hits + 1;
    }
}

println(hits);
