# 05_http_client

Cliente HTTP (`http_get`, `http_post`, `http_request`)
contra el servidor `--api` propio (`endpoint.te` en :9000) o
endpoints externos sobre HTTPS (libcurl).

Levantar el servidor primero:
  docker compose up -d typeeasy
