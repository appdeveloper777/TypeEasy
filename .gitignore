# Archivos generados por Flex/Bison
src/lex.yy.c
src/parser.tab.c
src/parser.tab.h
src/parser.output

# Ejecutables
typeeasy
!cli/typeeasy

# Object files y artefactos de build C/C++
*.o
*.obj
*.a
*.lib
*.exe
*.dll
*.dylib
.debug/

# WAHA Sessions (datos privados de WhatsApp)
waha_sessions/

# Variables de entorno (contienen API keys)
.env

# Logs
*.log

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# Node modules (si usas npm)
node_modules/

# Docker volumes
volumes/
nul

# VS Code extension package artifacts (regenerate with `vsce package`)
*.vsix

# Generated dumps / debug artifacts under src/ (do NOT commit)
src/parser.output.txt
src/plot_data.txt
src/revisit_dump.out
src/symtab_dump.out
src/*.out

# Test runner outputs
tests/.report/
test_output.txt

# Docker buildx local state (created by `docker buildx` / Docker Desktop)
.docker/

# MSYS2 native build-time shims (generated by scripts/build_native_windows.sh)
src/mysql/

# Proyecto chess/ — app demo separada, no es parte del core TypeEasy.
# Se versiona en su propio repo git local dentro de chess/.
chess/
.github_issue_v0.0.13.md

# Artefactos de debug/bench/smoke en la raíz (no versionar)
build_log.txt
build_tail.txt
copy_done.txt
grep_ws.txt
iso_out.txt
mem_out.txt
ping.txt
probe.txt
response.txt
smoke_out.txt
te_phase_d.txt

# Bases de datos SQLite de pruebas
*.db
*.db-shm
*.db-wal

# Scratch / probes / benchmarks generados bajo typeeasycode/
typeeasycode/_*.te
typeeasycode/_*.py
typeeasycode/probe*.te
typeeasycode/bench_csv_*.py
typeeasycode/*.csv

# CSV grandes generados para benchmarks (regenerables con benchmarks/gen_csv.py)
benchmarks/bench_*.csv
test_chess_repro.te
test_dup_params.te

# SQLite amalgamation descargada (plugin)
plugins/sqlite/sqlite-amalgamation-*/

# libFuzzer harness binary (build artifact of scripts/build_fuzzer.sh)
src/fuzz_parser

# App privada (no es parte del core TypeEasy, repo aparte)
ERP/
