# Archivos generados por Flex/Bison
src/lex.yy.c
src/parser.tab.c
src/parser.tab.h
src/parser.output

# Ejecutables
typeeasy
!cli/typeeasy

# Object files y artefactos de build C/C++
*.o
*.obj
*.a
*.lib
*.exe
*.dll
*.dylib
.debug/

# WAHA Sessions (datos privados de WhatsApp)
waha_sessions/

# Variables de entorno (contienen API keys)
.env

# Logs
*.log

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# Node modules (si usas npm)
node_modules/

# Docker volumes
volumes/
nul

# VS Code extension package artifacts (regenerate with `vsce package`)
*.vsix

# Generated dumps / debug artifacts under src/ (do NOT commit)
src/parser.output.txt
src/plot_data.txt
src/revisit_dump.out
src/symtab_dump.out
src/*.out

# Test runner outputs
tests/.report/
test_output.txt

# Docker buildx local state (created by `docker buildx` / Docker Desktop)
.docker/

# MSYS2 native build-time shims (generated by scripts/build_native_windows.sh)
src/mysql/

# Proyecto chess/ — app demo separada, no es parte del core TypeEasy.
# Se versiona en su propio repo git local dentro de chess/.
chess/
.github_issue_v0.0.13.md
